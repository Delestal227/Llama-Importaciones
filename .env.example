# Puerto (Render lo asigna automáticamente)
PORT=3000

# Base de datos (cuando integres)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=llama_importaciones

# Stripe o método de pago (cuando integres transacciones)
STRIPE_SECRET_KEY=
STRIPE_PUBLIC_KEY=

# Email (para formularios de contacto)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# JWT (para autenticación futura)
JWT_SECRET=
